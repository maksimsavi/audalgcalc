export function fetchRequest(){console.log("fetchrequest function works")}
export function receiveData(){
/*this will possibly process data.
depends on how the Fetch request pans out - I'll have to look at how it's built,
how to handle errors..
once fetch is done this will maybe parse, then pass the data to "displayData"

*/
}
export function displayTrackInfo(dummyData){
    console.log(dummyData);
}